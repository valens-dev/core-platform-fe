import { tss } from '@styles/theme';

const useStyles = tss.create(() => ({
  modalWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));

export default useStyles;
